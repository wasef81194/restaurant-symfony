{% extends 'base.html.twig' %}

{% block title %}Nouveau produit{% endblock %}

{% block body %}
{% include 'layout/navbar.html.twig' %}
    <div class="card w-50 all-center">
        <div class="card-body">
            <h1 class="card-title" style="margin:10px">Nouveau produit</h1>
            {{ include('produit/_form.html.twig') }}
            <a href="{{ path('restaurant_show',{'id': restaurant.id}) }}" class="btn btn-warning right">Annuler</a>
            </br></br>
        </div>
    </div>
{% endblock %}
