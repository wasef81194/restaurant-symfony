{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %} 
{% include 'layout/navbar.html.twig' %} 
    <div class="card w-50 all-center">
        <div class="card-body">
            <h1 class="card-title" style="margin:10px">Modifier votre profil</h1>

            {{ include('user/_form.html.twig', {'button_label': 'Modifier'}) }}

            <a href="{{ path('user_show', {'id': app.user.id}) }}" class='btn btn-warning left'>Annuler</a>

            <div class="right"> {{ include('user/_delete_form.html.twig') }} </div>
        </br></br>
        </div>
    </div>
{% endblock %}
